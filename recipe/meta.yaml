{% set name = "pywaterml" %}

{% set version = "1.2.26" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:

  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: bd7f9b09167a70e3ba5a4e3a8dfa2369410595e29a81c15a534c7ef3efad4c9a

build:

  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools
  run:
    - python >=3.6
    - numpy
    - owslib
    - pandas
    - suds-community
    - tslearn
    - xmltodict
    - pyproj
    - cython
test:
  imports:
    - pywaterml
    - pywaterml.waterML
    - pywaterml.auxiliaryMod
    - pywaterml.analyzeData

  source_files:
    - tests/test_fast.py
  requires:
    - pytest
  commands:
    - pytest -vv tests/test_fast.py

about:
  home: https://github.com/romer8/pywaterml
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: The pywaterml is a package that lets you handle WaterML functions such as GetValues, GetSitesInfo, etc. In addition it lets offers extra functions such as mean interpolation for data with gaps
  doc_url: https://pywaterml.readthedocs.io/en/latest/index.html
  dev_url: https://hydroinformatics.byu.edu/

extra:
  recipe-maintainers:
    - romer8
